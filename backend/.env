# 数据库配置
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=940927syz
DB_NAME=logtool

# JWT 密钥
JWT_SECRET=your_jwt_secret

# 其他配置
PORT=3000

# 队列配置
QUEUE_CONCURRENCY=3

# 队列分离配置
# 实时处理队列配置（用户请求）
REALTIME_QUEUE_CONCURRENCY=3
REALTIME_QUEUE_TIMEOUT_MS=300000

# 历史处理队列配置（自动上传）
# 历史处理对数据库写入压力较大，建议并发数保持较低（1~2）
HISTORICAL_QUEUE_CONCURRENCY=1
HISTORICAL_QUEUE_TIMEOUT_MS=600000

# 手术分析队列配置
SURGERY_QUEUE_CONCURRENCY=3
SURGERY_QUEUE_TIMEOUT_MS=600000

# 队列管理配置
QUEUE_MANAGER_ENABLED=true
QUEUE_STATUS_CHECK_INTERVAL=30000
QUEUE_CLEANUP_INTERVAL=3600000
QUEUE_MAX_RETENTION_HOURS=24

# 多进程集群配置（建议使用明确数字避免回退）
WORKER_PROCESSES=5
AUTO_RESTART_ENABLED=true
MAX_RESTART_ATTEMPTS=5
RESTART_DELAY=1000
HEARTBEAT_TIMEOUT=30000

# 数据库连接池（主进程读为主，工作进程写为主，同一 MySQL 实例）
# 主进程（API / 查询）
READER_DB_POOL_MAX=20
READER_DB_POOL_MIN=2
# 工作进程（队列 / 写入）
WRITER_DB_POOL_MAX=5
WRITER_DB_POOL_MIN=0
# 通用池参数
DB_POOL_ACQUIRE_MS=30000
DB_POOL_IDLE_MS=10000

# 队列调度配置
QUEUE_MAX_CONCURRENCY=10             # 被 userQueueManager 使用
QUEUE_SCHEDULER_INTERVAL=100         # 调度器检查间隔（毫秒）
QUEUE_MAX_ATTEMPTS=3                 # 最大重试次数
QUEUE_BACKOFF_DELAY=2000             # 重试延迟（毫秒）
# 队列超时时间配置（毫秒）
QUEUE_TIMEOUT_MS=300000              # 5分钟

# PostgreSQL配置
POSTGRES_USER=postgres
POSTGRES_PASSWORD=940927syz
POSTGRES_DB=logtool
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_SSL=false

# Redis缓存配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# 缓存功能配置（已使用）
CACHE_ENABLED=true
CACHE_TTL_SECONDS=300
CACHE_MAX_KEYS=500
SEARCH_CACHE_TTL_SECONDS=180

# 自动缓存清理配置（已使用）
AUTO_CACHE_CLEANUP=true
CACHE_CLEANUP_INTERVAL=300000
CACHE_METRICS_LIMIT=100
CACHE_ALERTS_LIMIT=50
CACHE_SEARCH_LIMIT=200

# 速率限制配置
RATE_LIMIT_ENABLED=true
RATE_LIMIT_WINDOW_MS=30000
RATE_LIMIT_MAX_REQUESTS=50
RATE_LIMIT_SKIP_SUCCESSFUL_REQUESTS=false
RATE_LIMIT_SKIP_FAILED_REQUESTS=false

# 智能调度配置（已使用）
INTELLIGENT_SCHEDULER_ENABLED=true
PEAK_HOURS_START=8
PEAK_HOURS_END=1
OFF_PEAK_HOURS_START=2
OFF_PEAK_HOURS_END=7

# 进程分配配置（PEAK_USER_REQUEST_WORKERS 留空表示“其余全部”）
PEAK_HISTORY_LOG_WORKERS=1
PEAK_USER_REQUEST_WORKERS=
OFF_PEAK_HISTORY_LOG_WORKERS=0.5
OFF_PEAK_USER_REQUEST_WORKERS=0.5

# 监控系统配置（已使用）
MONITORING_STORAGE_ENABLED=true          # 启用监控数据存储
MONITORING_RETENTION_DAYS=7              # 历史数据保留天数
MONITORING_BATCH_SIZE=100                # 批量存储大小
MONITORING_FLUSH_INTERVAL=60000          # 数据刷新间隔(毫秒)

# 流式处理配置
USE_STREAM_PROCESSING=true               # 是否启用流式处理（true/false）
STREAM_BATCH_SIZE=5000                   # 流式处理每批行数（影响 flush 频率）
STREAM_INSERT_BATCH_SIZE=300             # 流式处理每批插入数量（单批事务大小，建议 300~500）
STREAM_LARGE_FILE_THRESHOLD=20000        # 大文件阈值（超过此数量自动使用流式处理）

ENABLE_LOG_MAINTENANCE=false

# ClickHouse（日志明细存储）
CLICKHOUSE_HOST=http://47.98.156.69:8123
CLICKHOUSE_DB=logtool
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=940927syz            # 如果没有设置密码就留空；如果给 default 设置了密码就填这里

# 讯飞机器翻译 API 配置（用于故障码多语言自动翻译）
# 获取API密钥：https://console.xfyun.cn/services/bm35
IFLYTEK_APPID=0642b827
IFLYTEK_API_KEY=118f144bd0cf7a000e7f471b5176fc46
IFLYTEK_API_SECRET=OTA2MWMwMjg3ZjY0ZmFjNDcyZjMyOWIw

# 钉钉登录配置（请填写实际值）
# 内部应用/H5 免登使用的 AppKey + Secret
DINGTALK_APP_KEY=dingfouyuuyptqu0jsv7
DINGTALK_APP_SECRET=Fh6UIsoX_wMOdejebyGJOzu1O2jRcTTUq6_zbYDu-4Ykgy8qvDpXpMl3CYsXHkqx

# 网站应用扫码登录用的 AppId（ClientId）+ ClientSecret
DINGTALK_APP_ID=4eb5ef9c-2b97-4008-8f25-adb28881df33
# 建议为网站应用单独配置 ClientSecret，避免与内部应用冲突
DINGTALK_WEB_APP_SECRET=

# 扫码登录回跳地址，例如 https://your-domain/login
DINGTALK_LOGIN_REDIRECT=http://192.168.71.7:8080/login

# local配置
TECH_SOLUTION_LOCAL_DIR=D:/code/Log/v0.1.1/logtool/backend/uploads/tech-solution
TECH_SOLUTION_PUBLIC_BASE=/static/tech-solution
TECH_SOLUTION_STORAGE=local
# OSS配置（oss模式时必需）
OSS_REGION=oss-cn-hangzhou
OSS_ACCESS_KEY_ID=your_key
OSS_ACCESS_KEY_SECRET=your_secret
OSS_BUCKET=your-bucket
TECH_SOLUTION_OSS_PREFIX=tech-solution/
TECH_SOLUTION_OSS_BASE_URL=https://your-cdn.com

TECH_SOLUTION_MAX_SIZE=20971520
TECH_SOLUTION_ALLOWED_MIMES=image/jpeg,image/png,image/gif,image/webp,image/bmp,application/pdf