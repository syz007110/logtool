# 2026-04-19 后端暂缓任务归档

## 归档说明

- 本文档记录当前轮次暂不处理的后端任务。
- `AGENTS.md` 仅保留“当前需要做的任务”，其余进入本归档。

## 暂缓任务

1. `/api/agent/execute` 与 `/api/agent/tasks/:taskId` 增加 `auth + permission`
- 当前状态：未开始
- 暂缓原因：需先明确权限模型（建议 `agent:execute` / `agent:read` 或复用现有权限）
- 恢复条件：权限键方案确认后实施

2. 钉钉 webhook 请求来源校验（签名/时间戳/secret）
- 当前状态：未开始
- 暂缓原因：需确认企业侧签名规范与环境变量配置策略
- 恢复条件：明确签名算法与回放窗口策略

3. `query token` 白名单进一步收敛
- 当前状态：部分完成（已限制到白名单，已兼容 Jira 代理）
- 暂缓原因：前端仍存在多个 URL 直连场景，需统一改造为 Header 方案后再彻底收敛
- 恢复条件：前端附件/图片链路完成 Header 化改造

4. 队列关键任务幂等策略说明与测试补齐
- 当前状态：未完成
- 暂缓原因：需要梳理各队列任务的幂等键设计（含 `jobId` 去重策略）
- 恢复条件：完成队列任务清单与幂等键设计评审

## 已完成（供追踪）

- 队列路由已加鉴权与读写分权。
- 认证中间件已移除敏感日志输出。
- `query token` 已从“任意 GET”收敛为“白名单 GET 接口”。
